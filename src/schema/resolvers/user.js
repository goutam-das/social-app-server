import { Post } from '../../models/Post';

export default {
    Mutation: {
        register: async (_, args) => {
            try {
                
            } catch (err) {
                throw new Error(err);
            }
        }
    }
}